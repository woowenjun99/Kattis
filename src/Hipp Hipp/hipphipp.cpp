#include <iostream>

void solve() {
    for (int i = 0; i < 20; ++i) std::cout << "Hipp hipp hurra!\n";
}

int main(void) {
    std::ios::sync_with_stdio(0); 
    std::cin.tie(0); 
    std::cout.tie(0);
    solve();
    return 0;
}