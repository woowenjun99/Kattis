#include <iostream>
#include <vector>

void solve() {
    int m, n;
    std::cin >> m >> n;
}

int main(void) {
    std::ios::sync_with_stdio(0); 
    std::cin.tie(0); 
    std::cout.tie(0);
    solve();
    return 0;
}